{
  "name": "flowl",
  "version": "0.1.0",
  "private": true,
  "description": "Flowl study timer and owl care app.",
  "scripts": {
    "prepare:www": "node scripts/prepare-www.mjs",
    "cap:sync": "npm run prepare:www && npx cap sync",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
